{{- $email := get . "email" -}}
{{- if not $email -}}
{{-   $email = promptString "email" -}}
{{- end -}}
{{- $name := get . "name" -}}
{{- $signingkey := get . "signingkey" -}}
{{- if not $name -}}
{{-   $name = promptString "name" -}}
{{- end -}}

[data]
  email = {{ $email | quote }}
  name = {{ $name | quote }}
